import React from "react";
import { RepoCard } from '../../components';

const Repo = () => {

    // the RepoCard needs the repoName props !!!

    return(
        <>
            <RepoCard/>
        </>
    )
};

export default Repo;
