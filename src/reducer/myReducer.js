

const myReducer = (state=initState, action) => {
    switch(action.type){
        case 'LOAD_RESULT': 
            return 
        default:
            return state
    }
}

export default myReducer